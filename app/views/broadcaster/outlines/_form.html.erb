<%#= form_with(url: broadcaster_theme_outlines_path, local: true) do |form| %>
<%= form_with(scope: :broadcaster_outline, url: broadcaster_theme_outlines_path, class: "outline", local: true) do |form| %>
  <% if broadcaster_outline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(broadcaster_outline.errors.count, "error") %> prohibited this broadcaster_outline from being saved:</h2>

      <ul>
      <% broadcaster_outline.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :body %>
    <%= form.text_area :body, class: 'form-control' %>
  </div>

  <div class="form-group">
    <% if broadcaster_outline.persisted? %>
      <div class="float-right">
        <%= link_to 'Destroy', broadcaster_outline, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if broadcaster_outline.persisted? %>
      <%= link_to "Cancel", broadcaster_theme_outline_path, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", broadcaster_theme_outlines_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
