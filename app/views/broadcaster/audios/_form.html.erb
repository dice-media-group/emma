<%= form_with(scope: :broadcaster_audio, url: broadcaster_theme_audios_path, local: true) do |form| %>
  <% if broadcaster_audio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(broadcaster_audio.errors.count, "error") %> prohibited this broadcaster_audio from being saved:</h2>

      <ul>
      <% broadcaster_audio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, class: 'form-control' %>
  </div>

  <div class="form-group">
    <% if broadcaster_audio.persisted? %>
      <div class="float-right">
        <%= link_to 'Destroy', broadcaster_audio, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if broadcaster_audio.persisted? %>
      <%= link_to "Cancel", broadcaster_theme_audio_path, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", broadcaster_theme_audios_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
