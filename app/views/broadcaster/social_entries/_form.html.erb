<%= form_with(model: broadcaster_social_entry, local: true) do |form| %>
  <% if broadcaster_social_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(broadcaster_social_entry.errors.count, "error") %> prohibited this broadcaster_social_entry from being saved:</h2>

      <ul>
      <% broadcaster_social_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :broadcaster_theme_id %>
    <%= form.text_field :broadcaster_theme_id, class: 'form-control' %>
  </div>

  <div class="form-group">
    <% if broadcaster_social_entry.persisted? %>
      <div class="float-right">
        <%= link_to 'Destroy', broadcaster_social_entry, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if broadcaster_social_entry.persisted? %>
      <%= link_to "Cancel", broadcaster_social_entry, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", broadcaster_social_entries_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
