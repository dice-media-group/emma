<%= form_with(model: broadcaster_theme_avatar, local: true) do |form| %>
  <% if broadcaster_theme_avatar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(broadcaster_theme_avatar.errors.count, "error") %> prohibited this broadcaster_video from being saved:</h2>

      <ul>
      <% broadcaster_theme_avatar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, class: 'form-control' %>
  </div>

  <div class="form-group">
    <% if broadcaster_theme_avatar.persisted? %>
      <div class="float-right">
        <%= link_to 'Destroy', broadcaster_theme_avatar, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if broadcaster_theme_avatar.persisted? %>
      <%= link_to "Cancel", broadcaster_theme_avatar, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", broadcaster_theme_avatars_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
