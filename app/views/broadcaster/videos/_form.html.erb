
<%= form_with(scope: :broadcaster_video, url: broadcaster_theme_videos_path, class: "video", local: true) do |form| %>
  <% if broadcaster_video.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(broadcaster_video.errors.count, "error") %> prohibited this broadcaster_video from being saved:</h2>

      <ul>
      <% broadcaster_video.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, class: 'form-control' %>
  </div>

  <div class="form-group">
    <% if broadcaster_video.persisted? %>
      <div class="float-right">
        <%= link_to 'Destroy', broadcaster_video, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if broadcaster_video.persisted? %>
      <%= link_to "Cancel", broadcaster_theme_video, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", broadcaster_theme_videos_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>