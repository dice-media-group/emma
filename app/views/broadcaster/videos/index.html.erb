


<div class="row">
  <div class="col-sm-6">
    <h1>Broadcaster_videos</h1>
  </div>

  <div class="col-sm-6 text-right">
  <%= link_to new_broadcaster_theme_video_path, class: 'btn btn-primary' do %>
    Add New Video
  <% end %>
  </div>
</div>

<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
    

            <th>Title</th>
            
          <th></th>
        
      </tr>
    </thead>

    <tbody>
      <% @broadcaster_videos.each do |broadcaster_video| %>
        <%= content_tag :tr, id: dom_id(broadcaster_video), class: dom_class(broadcaster_video) do %>
          

                      <td><%= broadcaster_video.title %></td>
          
          
            <td><%= link_to 'Show', broadcaster_video %></td>
          
        <% end %>
      <% end %>
    </tbody>
  </table>
</div>
