<%= form_with(scope: :broadcaster_theme_avatar, url: broadcaster_theme_theme_avatars_path, local: true) do |form| %>
  <% if broadcaster_theme_avatar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(broadcaster_theme_avatar.errors.count, "error") %> prohibited this broadcaster_theme_avatar from being saved:</h2>

      <ul>
      <% broadcaster_theme_avatar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :theme_avatar %>

    <%= form.select :broadcaster_avatar_id, Broadcaster::Avatar.all.collect { |avatar| [ avatar.name, avatar.id ] }, include_blank: true %>

  </div>

  <div class="form-group">
    <% if broadcaster_theme_avatar.persisted? %>
      <div class="float-right">
        <%= link_to 'Destroy', broadcaster_theme_avatar, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if broadcaster_theme_avatar.persisted? %>
      <%= link_to "Cancel", broadcaster_theme_theme_avatar_path, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", broadcaster_theme_theme_avatars_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
