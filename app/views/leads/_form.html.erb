<style>
 .form-group {
   margin: 10px
 }
</style>

<h1>Contact us</h1>

<%= form_for @lead , url: lead_path do |form| %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :phone %>
    <%= form.text_field :phone, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :email_address %>
    <%= form.text_field :email_address, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :subject %>
    <%= form.text_field :subject, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :message_body %>
    <%= form.text_area :message_body, class: 'form-control' %>
  </div>

  <div class="form-group">
    <% if lead.persisted? %>
      <div class="float-right">
        <%= link_to 'Destroy', lead, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit "Send Message", class: 'btn btn-primary' %>


<% end %>
