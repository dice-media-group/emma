<%= form_with(model: media_appearance, local: true) do |form| %>
  <% if media_appearance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(media_appearance.errors.count, "error") %> prohibited this media_appearance from being saved:</h2>

      <ul>
      <% media_appearance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :published_on %>
    <%= form.datetime_select :published_on, class: 'form-control' %>
  </div>

  <div class="form-group">
    <% if media_appearance.persisted? %>
      <div class="float-right">
        <%= link_to 'Destroy', media_appearance, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if media_appearance.persisted? %>
      <%= link_to "Cancel", media_appearance, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", media_appearances_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
