<%= form_with(model: blog_article, local: true) do |form| %>
  <% if blog_article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog_article.errors.count, "error") %> prohibited this blog_article from being saved:</h2>

      <ul>
      <% blog_article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<%# 

 %>
 <script>
      // In your Javascript (external .js resource or <script> tag)
    $(document).ready(function() {
        $('.js-example-basic-single').select2();
    });
  </script>
  <div class="form-group">

    <%= form.label :entry_id %>

    <select class="js-example-basic-single" 
      name="blog_article[entry_id]" 
      id="blog_article_entry_id">
        <% @entries.each do |a| %>
          <option value="<%= a.id %>"><%= a.title %> </option>    
        <% end %>    
    </select>
  </div>

<%#  %>
  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= form.label :pinned_value %> <p>(set to zero for it not to be pinned)</p>
    <%= form.number_field :pinned_value, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :body %>
    <%= form.rich_text_area :body, class: 'form-control' %>
  </div>

  <div class="form-group">
    <% if blog_article.persisted? %>
      <div class="float-right">
        <%= link_to 'Destroy', blog_article, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if blog_article.persisted? %>
      <%= link_to "Cancel", blog_article, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", blog_articles_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
